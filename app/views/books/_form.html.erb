<%= form_with model: @book do |form| %>
  <div><%= form.text_field :title, placeholder: 'title' %></div>
  <div><%= form.text_field :author, placeholder: 'author' %></div>
  <div><%= form.file_field :image %></div>
  <div><%= form.submit %></div>
<% end %>