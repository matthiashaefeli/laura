<%= form_with model: @comment do |form| %>
  <%= form.hidden_field :book_id, value: @book.id %>
  <div><%= form.text_area :text, placeholder: 'title' %></div>
  <div><%= form.submit %></div>
<% end %>